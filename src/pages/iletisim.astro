---
import BaseLayout from '@/layouts/BaseLayout.astro';
---

<BaseLayout title="İletişim" description="Prof. Dr. Elif Akkaş Yılmaz ile iletişime geçin.">

  <!-- ── PAGE HEADER ──────────────────────────────────────────── -->
  <div class="bg-forest text-ivory py-16">
    <div class="max-w-5xl mx-auto px-6 text-center">
      <p class="font-sans text-xs uppercase tracking-[0.25em] text-gold mb-3">İletişim</p>
      <h1 class="font-serif text-4xl font-bold">Bana Ulaşın</h1>
      <div class="w-16 h-px bg-gold mx-auto mt-6"></div>
    </div>
  </div>

  <div class="section max-w-3xl">

    <p class="font-sans text-warm/70 text-center mb-12 leading-relaxed">
      Sorularınızı ve görüşlerinizi benimle paylaşabilirsiniz. Tıbbi acil durumlarda
      lütfen en yakın sağlık kuruluşuna başvurun.
    </p>

    <!--
      FORMSPREE KURULUMU:
      1. https://formspree.io adresine gidin ve ücretsiz hesap açın.
      2. Yeni bir form oluşturun ve size verilen form ID'sini kopyalayın.
      3. Aşağıdaki action URL'ini kendi ID'nizle güncelleyin:
         action="https://formspree.io/f/FORM_ID_BURAYA"
    -->
    <form
      action="https://formspree.io/f/xykdlbze"
      method="POST"
      class="space-y-6"
    >
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <label for="name" class="block font-sans text-xs uppercase tracking-widest text-warm/60 mb-2">
            Ad Soyad <span class="text-gold">*</span>
          </label>
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full border border-sage/30 bg-white px-4 py-3 font-sans text-sm text-warm focus:outline-none focus:border-sage transition-colors"
            placeholder="Adınız Soyadınız"
          />
        </div>

        <div>
          <label for="email" class="block font-sans text-xs uppercase tracking-widest text-warm/60 mb-2">
            E-posta <span class="text-gold">*</span>
          </label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full border border-sage/30 bg-white px-4 py-3 font-sans text-sm text-warm focus:outline-none focus:border-sage transition-colors"
            placeholder="ornek@eposta.com"
          />
        </div>
      </div>

      <div>
        <label for="subject" class="block font-sans text-xs uppercase tracking-widest text-warm/60 mb-2">
          Konu
        </label>
        <input
          type="text"
          id="subject"
          name="subject"
          class="w-full border border-sage/30 bg-white px-4 py-3 font-sans text-sm text-warm focus:outline-none focus:border-sage transition-colors"
          placeholder="Mesajınızın konusu"
        />
      </div>

      <div>
        <label for="message" class="block font-sans text-xs uppercase tracking-widest text-warm/60 mb-2">
          Mesajınız <span class="text-gold">*</span>
        </label>
        <textarea
          id="message"
          name="message"
          required
          rows="6"
          class="w-full border border-sage/30 bg-white px-4 py-3 font-sans text-sm text-warm focus:outline-none focus:border-sage transition-colors resize-none"
          placeholder="Mesajınızı buraya yazın..."
        ></textarea>
      </div>

      <!-- Honeypot (spam koruması) -->
      <input type="text" name="_gotcha" class="hidden" />

      <div class="text-center pt-2">
        <button type="submit" class="btn-primary">
          Gönder
        </button>
      </div>
    </form>

  </div>

</BaseLayout>
